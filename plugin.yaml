name: kb_journeys
version: 0.1.0
description: "Telegram-native /kb status, /kb sync, and /kb review cards backed by KB MCP packets and proposal receipts."
author: "Hermes Agent"
provides_hooks:
  - pre_gateway_dispatch
  - post_llm_call
