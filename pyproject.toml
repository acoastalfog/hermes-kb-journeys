[project]
name = "hermes-kb-journeys"
version = "0.10.8"
description = "Out-of-tree Hermes plugin for guided KB journeys."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
